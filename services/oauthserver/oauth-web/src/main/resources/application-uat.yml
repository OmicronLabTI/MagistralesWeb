spring:
  datasource:
    url: jdbc:postgresql://omicron.postgres.database.azure.com:5432/omicronprod?user=omicronadmin@omicron&password=e0F6gH4CfrBeZdiO.2020&sslmode=require
    driver-class-name: org.postgresql.Driver
    hikari:
      maximumPoolSize: 1000
      minimumIdle: 3
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  application:
    name: oauth-service
  profiles: qa

server:
  port: 8090

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://${APIMANAGER_SVC_SERVICE_HOST}:${APIMANAGER_SVC_SERVICE_PORT}/eureka
  instance:
    leaseRenewalIntervalInSeconds: 5
    preferIpAddress: true

security:
    values:
      secret: Prueba
      ttl: 43200
      ttl-refresh: 604800